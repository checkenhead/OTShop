<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ot.team1.dao.IAdminDao">

	<resultMap id="adminMap" type="java.util.HashMap"></resultMap>
	
	<select id="getAdmin" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getAdmin(
			#{adminid},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getAllProductCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProductCatList(
			#{categoryClass},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<!-- <insert id="insertProduct" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProduct(
			#{optionList, javaType=Object, jdbcType=ARRAY, jdbcTypeName=OPT_TABLE_TYPE, mode=IN, typeHandler=ot.team1.mybatis.ListMapTypeHandler},
			#{pseq, mode=OUT, jdbcType=INTEGER})
		}
		
	</insert> -->
	
	<insert id="insertProduct" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProduct(
			#{BRAND}, #{NAME}, #{DESCRIPTION}, #{GENDER}, #{IMAGE},
			#{pseq, mode=OUT, jdbcType=INTEGER})
		}
	</insert>
	
	<insert id="insertProductDetail" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProductDetail(
			#{PSEQ}, #{OPTNAME}, #{PRICE1}, #{PRICE2}, #{PRICE3}, #{STOCK})
		}
	</insert>
	
	<!-- productDao로 이동 -->
	<!-- <select id="getProductList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProductList(
			#{keyword},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getProductDetailList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProductDetailList(
			#{PSEQ},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select> -->
	
	<select id="getCount" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getCount(
			#{tablename}, #{keynum},
			#{count, mode=OUT, jdbcType=INTEGER})
		}
	</select>
	
	<!-- 제품 카테고리 변경
	<insert id="insertProductCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProductCat(#{name})
		}
	</insert>
	-->
	
	<insert id="insertProductCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProductCat(#{tablename}, #{name})
		}
	</insert>
	
	<delete id="deleteProductCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteProductCat(#{tablename}, #{index})
		}
	</delete>
	
	<update id="updateProductCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateProductCat(#{tablename}, #{index}, #{name})
		}
	</update>
	
	<!-- productDao로 이동 -->
	<!-- <select id="getProduct" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProduct(
			#{pseq},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select> -->
	
	<update id="updateProduct" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateProduct(
			#{PSEQ}, #{BRAND}, #{NAME}, #{DESCRIPTION}, #{GENDER}, #{IMAGE}, #{BESTYN}, #{USEYN})
		}
	</update>
	
	<insert id="updateProductDetail" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateProductDetail(#{PDSEQ}, #{STORE}, #{USEYN})
		}
	</insert>
	
	<select id="getFaqList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getFaqList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getFaqCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getFaqCatList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<insert id="insertFaqCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertFaqCat(#{name})
		}
	</insert>
	
	<delete id="deleteFaqCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteFaqCat(#{fcseq})
		}
	</delete>
	
	<update id="updateFaqCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateFaqCat(#{fcseq}, #{name})
		}
	</update>
	
	<insert id="insertFaq" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertFaq(
			#{fcseq}, #{title}, #{content})
		}
	</insert>
	
	<update id="updateFaq" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateFaq(
			#{fseq}, #{fcseq}, #{title}, #{content})
		}
	</update>
	
	<delete id="deleteFaq" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteFaq(#{fseq})
		}
	</delete>
	
	<select id="getMemberList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getMemberList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<update id="changeMemberUseyn" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call changeMemberUseyn(#{userid}, #{useyn})
		}
	</update>
	
	<select id="getQnaList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getQnaList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getQnaCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getQnaCatList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<insert id="insertQnaCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertQnaCat(#{name})
		}
	</insert>
	
	<delete id="deleteQnaCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteQnaCat(#{qcseq})
		}
	</delete>
	
	<update id="updateQnaCat" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateQnaCat(#{qcseq}, #{name})
		}
	</update>
	
	<select id="getQna" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getQna(
			#{qseq},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<update id="updateQnaReply" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateQnaReply(#{qseq}, #{reply})
		}
	</update>
	
	<delete id="deleteQna" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteQna(#{qseq})
		}
	</delete>
	
	<insert id="insertBannerImage" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertBannerImage(#{name}, #{image})
		}
	</insert>
	
	<select id="getBannerImageList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getBannerImageList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<delete id="deleteBannerImage" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteBannerImage(#{biseq})
		}
	</delete>
	
	<select id="getImageByBiseq" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getImageByBiseq(
			#{biseq}, #{image, mode=OUT, jdbcType=VARCHAR})
		}
	</select>
	
	<insert id="insertBanner" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertBanner(#{biseq}, #{uri}, #{useyn}, #{priority})
		}
	</insert>
	
	<select id="getBannerList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getBannerList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<delete id="deleteBanner" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteBanner(#{bseq})
		}
	</delete>
	
	<update id="updateBanner" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateBanner(#{bseq}, #{uri}, #{useyn}, #{priority})
		}
	</update>
	
	<select id="getMaxPriority" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getMaxPriority(
			#{maxpriority, mode=OUT, jdbcType=INTEGER})
		}
	</select>
	
	<select id="getBannerPriorityList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getBannerPriorityList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<update id="updateBannerPriority" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateBannerPriority(#{bseq}, #{priority})
		}
	</update>
	
	<insert id="insertProductMainCatSet" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProductMainCatSet(#{pmcseq},
			#{pmcsseq, mode=OUT, jdbcType=INTEGER})
		}
	</insert>
	
	<insert id="insertProductSubCatSet" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertProductSubCatSet(#{pmcsseq}, #{pscseq})
		}
	</insert>
	
	<select id="getCategorySetList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getCategorySetList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<!-- productDao로 이동 -->
	<!-- <select id="getMainCatSetList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getMainCatSetList(
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getSubCatSetList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getSubCatSetList(
			#{pmcsseq},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select> -->
	
	<update id="updateProductCatSet" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call updateProductCatSet(
			#{tablename}, #{index}, #{value})
		}
	</update>
	
	<delete id="deleteProductCatSet" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteProductCatSet(
			#{tablename}, #{index})
		}
	</delete>
	
	<insert id="insertMainCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertMainCatList(#{PSEQ}, #{PMCSEQ})
		}
	</insert>
	
	<insert id="insertSubCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call insertSubCatList(#{PSEQ}, #{PSCSEQ})
		}
	</insert>
	
	<select id="getProductMainCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProductMainCatList(
			#{pseq},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<select id="getProductSubCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call getProductSubCatList(
			#{pseq},
			#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=adminMap})
		}
	</select>
	
	<delete id="deleteMainCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteMainCatList(#{PMCLSEQ})
		}
	</delete>
	
	<delete id="deleteSubCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			call deleteSubCatList(#{PSCLSEQ})
		}
	</delete>
	
</mapper>