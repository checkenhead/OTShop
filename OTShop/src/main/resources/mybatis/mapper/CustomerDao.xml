<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="ot.team1.dao.ICustomerDao">


	<resultMap id="qnaMap" type="java.util.HashMap"></resultMap>
	
	
	<select id="listQna" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL listQna(
				#{rfcursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=qnaMap }
			)
		}
	</select>
	
	<select id="getQnaCatList" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL getQnaCatList(
				#{rfcursor2 , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=qnaMap }
			)
		}
	</select>
	
	<select id="getQnaCatListUser" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL getQnaCatListUser(
				#{qseq},
				#{rfcursor2 , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=qnaMap }
			)
		}
	</select>
	
	<select id="getQna" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL getQna(
				#{qseq},
				#{rfcursor , mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=qnaMap }
			)
		}
	</select>

	
	
</mapper>